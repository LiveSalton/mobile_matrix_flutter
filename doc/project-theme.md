# 主题规范

## 语义令牌

- 仅允许双主题：`defaultTheme`（液态玻璃蓝）与 `roseGlow`（玫瑰流光）。
- 界面组件一律使用 `context.tokens` 语义色值，禁止直接硬编码 Hex 颜色。

## 组件与交互

- 基础圆角规范为 6/8/10/14px，高光内描边采用 1px 微透明度。
- 点击目标与交互区域最小不低于 36px，按键须提供即时悬停与按压态反馈。
- 严禁使用 Emoji 代替核心功能图标，统一使用 Material Round 图标库。
