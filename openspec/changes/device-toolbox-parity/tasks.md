# 实施任务

## 参考与规范

- [x] 1.1 固化 Web 端七组设备工具、控制服务行为和 Flutter 桌面差异。
- [x] 1.2 明确截图仅复制图片剪贴板、文件拉取使用目标路径、群控/Run JS/VNC 的范围边界。
- [x] 1.3 完成 Reference Packet、OpenSpec 和实现计划的双向追踪校验。

## 工具服务与模型

- [x] 2.1 添加设备工具导航、文件、信息、端口和结果模型，并为解析器写失败测试。
- [x] 2.2 实现 ADB host/shell 通用执行、包安装卸载、应用列表、远程调试和端口转发。
- [x] 2.3 实现 logcat 子进程的启动/停止/过滤输入和 dispose 清理。
- [x] 2.4 实现目录解析、文件拉取和设备信息聚合读取。
- [x] 2.5 实现铃声、Wi‑Fi、蓝牙、蓝牙配对清理、应用商店和设备重启命令映射。

## Flutter 界面

- [x] 2.6 将 DeviceWorkspace 改为六组工具导航 + 单内容区，保留现有 Dashboard 功能。
- [x] 2.7 接入 Logs、Automation 和 File Explorer 工具状态与操作反馈。
- [x] 2.8 接入 Advanced、Info、应用管理和远程调试界面，移除所有群控入口。
- [x] 2.9 让 DeviceControlPage 创建、切换和销毁 DeviceToolsService。
- [x] 2.10 在最小窗口和窄内容区使用滚动/Wrap/Expanded，避免固定宽高越界。
- [x] 2.11 将设备信息栏和截屏图标移动到右栏工具箱顶部，左栏只保留手机画面和底部导航。
- [x] 2.12 将完整 AppHeader 移动到右栏顶部，左栏只保留手机画面和底部导航。
- [x] 2.13 按设备屏幕方向和宽高比计算左栏宽度，让启动后的手机画面贴合左栏并保留右侧最小工作区宽度。
- [x] 2.14 按 Apple Design 原则优化右栏材质层级、留白、工具选中态、卡片、输入框和按钮反馈，不改变设备工具功能。
- [x] 2.15 统一 Quick Control Deck 四个控件的宽高，移除中英文双语标签并补充英文悬停提示。
- [x] 2.16 移除右侧独立的“控制矩阵工作台”标题栏，保留设备信息栏、工具导航和工具内容。
- [x] 2.17 为可滚动 Dashboard 中的快捷控制行增加有限高度约束，修复窗口拖动时的无界布局和连带鼠标追踪断言。
- [x] 2.18 固定 FPS 胶囊尺寸，移除快捷控制按钮可见文字，仅保留图标、悬停提示和无障碍标签。
- [x] 2.19 为 Real-time System Monitor 增加默认关闭的按需开关，接入 CPU/内存/网络 ADB 采样、5 秒轮询、历史折线和设备切换/页面销毁清理。
- [x] 2.20 固定实时监控卡片高度，统一快捷操作按钮为等宽等高方形，并调整设备信息栏顺序及横屏/显隐/复制截屏操作分组。
- [x] 2.21 修复横屏切换后的状态同步、画面比例保持和旧方向帧旋转；固定监控卡片内容高度，收缩剪贴板快捷行，并为窄右栏提供紧凑 AppHeader。
- [x] 2.22 将 Dashboard 卡片标题和可见辅助文案统一为中文，并修复高级剪贴板中心内容的左对齐基线。
- [x] 2.23 增加截屏复制链路日志；macOS 使用原生图片剪贴板写入并清理系统临时 PNG，避免第三方剪贴板写入状态与系统实际内容不一致。
- [x] 2.24 将 Dashboard 底部应用管理与 ADB 远程调试卡片按半宽顺排，并增加读取本机 ADB 设备、发现 `_adb-tls-connect` 动态端口和自动填充真实 `地址:端口` 的操作。
- [x] 2.25 重构 Dashboard 响应式卡片列表，将底部双卡片作为 `_toolScroll` 的单个响应式 item，并按项目 L10n 规范整理页面用户可见硬编码文案。
- [x] 2.26 重构 Dashboard 为双列独立卡片流，消除由不等高列造成的空白区域；宽屏每列半宽，窄屏合并为单列滚动。
- [x] 2.27 移除 DeviceWorkspace 独立截屏工具入口和页面，仅保留设备信息栏的复制截屏快捷图标；将 Wi‑Fi、蓝牙快捷开关移动到同一操作组。

## 验证

- [x] 3.1 按 TDD 运行设备工具纯逻辑测试，确认解析、校验和命令构造覆盖失败分支。
- [x] 3.2 执行 Dart format、Flutter analyze、OpenSpec 和 Reference Packet 校验。
- [x] 3.3 复核工作区差异，确认无录制、无截图落盘、无群控 Tab，保留真机验收清单。

## 用户验收

- [ ] 4.1 在已连接 Android 真机上逐项验证 ADB 操作、logcat、文件拉取、端口转发、设备信息和截图图片剪贴板。
- [ ] 4.2 在 `1120×760` 最小窗口和实际桌面主题下检查六组工具的视觉布局与拖动窗口稳定性。
