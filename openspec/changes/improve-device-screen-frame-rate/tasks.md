## 1. STF 屏幕流契约

- [x] 1.1 扩展屏幕流接口，提供可监听状态、脱敏错误信息和 Web 等价 viewport 请求参数。
- [x] 1.2 让页面装配优先传入设备模型已有的 STF 屏幕地址，并保留当前本机 STF 端口解析作为同协议兼容入口。
- [x] 1.3 将 `SmartScreenStreamService` 收敛为 STF WebSocket 唯一生产路径，移除自启 minicap 与 ADB 截图的实时画面回退。
- [x] 1.4 实现连接、重连、尺寸变化、隐藏和恢复时严格的 `size`、`on`、`off` 消息顺序及错误状态。

## 2. Web 等价投影与画面状态

- [x] 2.1 复刻 Web 的像素密度 1.0～1.5、最小缩放 0.36、横屏宽高交换和向上取整算法。
- [x] 2.2 在设备画面布局稳定后提交 viewport，并对相同整数投影尺寸去重，避免在 `build` 中直接发送网络消息。
- [x] 2.3 让画面舞台可观察连接状态并区分连接中、暂停、错误和断开，不以空白画面掩盖 STF 失败。

## 3. Flutter 等价帧渲染

- [x] 3.1 将解码队列限制为一个正在解码帧和一个最新待解码帧，并统计被覆盖的中间帧。
- [x] 3.2 使用 `ui.Codec` / `ui.Image` / `CustomPaint` 完成 JPEG 解码与绘制，释放编码缓冲、描述符、codec 和旧图片资源。
- [x] 3.3 使用 `RepaintBoundary` 隔离屏幕重绘，并分别展示接收 FPS、渲染 FPS、丢帧数和最近解码耗时。

## 4. 静态验证与交付

- [x] 4.1 检查生产调用链不再启动、杀死或直连 minicap，也不再把 ADB 截图作为实时画面回退。
- [x] 4.2 仅格式化本次涉及的 Dart 文件，执行 Dart 静态分析和 OpenSpec 严格校验；不构建、不发布、不修改测试。
- [x] 4.3 记录源码与静态验证证据，并提供相同设备、相同投影尺寸下由用户执行的 Web/Flutter 连续滚动验收清单。
